NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only. Used exclusively by /admin/metrics write routes to bypass
# RLS. Never expose this to the client or commit it.
SUPABASE_SERVICE_ROLE_KEY=

# Simple shared-password gate for /admin/metrics (Phase 2).
# TEMPORARY until the real auth system (Section 9 of the build brief) is built.
ADMIN_METRICS_PASSWORD=

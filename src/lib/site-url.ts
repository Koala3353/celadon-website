/**
 * The site's production origin. Used anywhere an absolute URL is required —
 * metadataBase, the sitemap, robots.txt, and structured data — none of which
 * can rely on a request object under `output: "export"`.
 */
export const SITE_URL = "https://ateneoceladon.com";
